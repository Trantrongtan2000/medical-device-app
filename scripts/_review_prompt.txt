[AI REVIEW REQUEST — PHASE 1 IMPLEMENTATION]

Cảm ơn đã hỗ trợ kế hoạch. Giờ cần review code đã implement. Các file chính đã thay đổi:

**Backend:**
- `app/routes_schedules.py` — CRUD maintenance_schedules + generate engine + alerts/expiring + notifications API (tương tự plan T1.2-T1.4)
- `database/schema.sql` — bổ sung 5 cột mới vào maintenance_schedules + bảng notifications

**Database (migration):**
- `database/devices.db` — đã ALTER maintenance_schedules + CREATE notifications
- Backup trong `database/backups/devices_before_phase1_*.db`

**Frontend:**
- `web/js/app.js` — thêm loadSchedules(), loadAlertsSummary(), showScheduleDetail() + init() gọi loadSchedules + loadAlertsSummary
- `web/index.html` — thêm alerts summary bar ngay header overview tab (id="alerts-summary")

**Kiểm tra:**
- Script test: `scripts/_test_phase1_api.py` → 22 checks PASSED
- Backend API Test: `/api/schedules/list`, `/api/alerts/summary`, `/api/alerts/expiring`, `/api/notifications` → OK

**Hỏi review:**
1. Có nên thêm pagination cho `/api/schedules/list` không? hiện limit=100 cố định.
2. Thêm rate limiting cho `/api/alerts/expiring`? nếu call quá nhiều (frontend polling 60s) có ảnh hưởng DB?
3. Migration script `scripts/migrate_phase1.py` idempotent? có cần ghi log vào bảng `schema_migrations` không?
4. Tên cột `next_due_at` trùng với `due_date` thật sự không gây nhầm lẫn? Có nên đổi `next_due_at` → `maintenance_due_date`?
5. Alert level CRITICAL/WARNING/INFO — có nên mapping màu sắc khác cho UI không? (hiện chỉ trong backend)
6. UI schedule tab còn cần thêm chức năng gì? (tạo lịch thủ công, filter, export xuống Excel...)

Trả lời ngắn gọn từng câu hỏi. Đánh dấu đề xuất cải tiến nào cần priority cao.